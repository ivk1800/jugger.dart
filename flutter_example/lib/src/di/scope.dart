import 'package:jugger/jugger.dart';

@scope
class ApplicationScope {
  const ApplicationScope._();
}

const ApplicationScope applicationScope = ApplicationScope._();

@scope
class ScreenScope {
  const ScreenScope._();
}

const ScreenScope screenScope = ScreenScope._();
