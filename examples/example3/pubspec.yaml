name: example3
description: A new Flutter project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  jugger: ^1.1.0-alpha

dev_dependencies:
  build_runner: ^1.7.2
  flutter_test:
    sdk: flutter
  jugger_generator:
    path: ../../jugger_generator

flutter: